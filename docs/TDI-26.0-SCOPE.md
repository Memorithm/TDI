# TDI-26.0 — BANC v888 Sparse Recurrent Stage-0 Scope

Status: **active Stage-0 bootstrap; not frozen; no final or confirmatory execution authorised**.

## Purpose

TDI-26.0 defines the scientific and software boundary for the BANC v888 sparse-recurrent programme before any task outcome is compared.

The only connectome source allowed by this line is **FlyWire BANC materialization v888**. Raw BANC data is not committed to TDI. TDI consumes only versioned, provenance-carrying topology artifacts produced through a qualified SciRust boundary.

## Stage-0 executable surface

Stage 0 may implement only:

- dataset/materialization identity;
- topology-arm identities;
- exact logical node/edge/dynamic-memory budgets;
- fail-closed matched-budget validation;
- intervention vocabulary;
- deterministic software tests;
- bootstrap integrity checks.

Stage 0 must not run a scientific topology comparison or generate a final/confirmatory result payload.

## Required topology controls

The future sparse comparison panel must preserve:

- C1 random sparse;
- C2 in/out-degree matched;
- C3 degree + reciprocity matched;
- C4 degree + reciprocity + modularity matched;
- C5 V888 structural prior.

C0 dense recurrence is a contextual reference only and is not required to share the sparse edge budget.

The scientific interpretation of any V888-specific effect requires C5 to survive the strongest applicable matched controls. C5 versus C1 alone is insufficient.

## Source semantics

TDI-26 does not define BANC file parsing. SciRust owns the V888 import/provenance contract and reusable graph primitives. The TDI evaluator must bind to an exact SciRust contract revision before any later preregistration freezes concrete topology artifacts.

## Resource accounting

Later comparisons must separately retain at least:

- node count;
- directed edge count;
- dynamic-memory bits;
- active-node count;
- active-edge count;
- event count;
- recurrent/event steps;
- task output;
- intervention identity;
- seed;
- exact source and dependency SHAs.

Static topology budget equality does not imply equal runtime work; active-resource counters remain separate observables.

## Stage transition

TDI-26.1 evaluator implementation remains blocked until:

1. TDI-26.0 Stage-0 scope/code/bootstrap are merged;
2. a later reviewed freeze pins the Stage-0 payload;
3. the freeze integrity gate passes on `main`;
4. the SciRust V888 contract used by the evaluator is an exact merged revision;
5. a separate TDI-26.1 preregistration freezes tasks, splits, metrics, rejection rules and evidence semantics.

Passing Stage 0 establishes contract consistency only. It is not evidence that V888 improves computation, robustness, model quality, memory, latency or generalization.
