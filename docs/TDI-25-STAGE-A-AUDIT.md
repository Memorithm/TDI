# TDI-25 Stage-A audit / freeze candidate

This artifact closes only the semantic and plumbing phase of TDI-25. It does not authorize any Development, Validation, protected/final, training, hardware-performance, or scientific-comparison execution.

The audit cross-checks:

- exact TDI-22 and TDI-24 source-contract pins;
- G6 identity and carrier accounting;
- common score scaling;
- torsor transport invariants through the TDI-25 bridge;
- chiral mirror/parity invariants through the TDI-25 bridge;
- identical T6/C6/G6 mask and normalizer path;
- typed comparison records retaining failure provenance;
- fail-closed malformed rows and non-finite successful records.

Stage-B may begin only after this candidate merges with exact-head qualification and no unresolved material review finding.
