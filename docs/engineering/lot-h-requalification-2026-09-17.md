# Lot H partner-boundary requalification — 2026-09-17

This record refreshes software-contract evidence for issue #278. It does not
rewrite historical merge-time state and does not promote any scientific,
hardware, actuation, protected-holdout or performance authority.

## Scope

The candidate re-runs the four current-main partner contract boundaries on one
exact head:

- ElasticXxx non-actuating resource-control interchange;
- SciRust reusable-primitive promotion metadata;
- FLAT-ATTENTION qualification/review metadata;
- NNIS hardware-qualification review metadata.

The common invariants remain fail-closed: exact repository/source/protocol
identity, exact G3-admitted Hub binding, root-artifact binding, JSON type
fidelity, Development/Validation domain alignment and hard-false authority bits.

## Independent upstream/runtime evidence

ElasticXxx #95 exact head `f8e10b1a1d05d6c22c0f56fed87252bc65d4e66a`
now has successful upstream `ci` and `packageability` workflow runs. TDI's
separate resource-admission workflow remains an independently qualified runtime
surface; it is not the same contract as the older non-actuating Lot H envelope.

The real TDI→SciRust analysis/sensitivity workflows use qualified SciRust #1452
merge `be7fcca3b31cedf722d71a2a56db8f6d088037cf`. The reusable-primitive
promotion contract retains its own historical audited API pin; neither pin is
silently substituted for the other.

FLAT and NNIS contract qualification remains review/interchange-only. In
particular the audited NNIS CUDA-Rust SIMT manifest remains
`unresolved_blocking`; a green TDI adapter test cannot manufacture missing
real-device evidence.

## Qualification rule

Local contract tests are supporting evidence only. Promotion of these current-
main contract implementations requires every applicable workflow on the exact
final PR head to complete successfully. Queued, missing, cancelled, stale-SHA or
failed checks are not success. The resulting exact head and workflow runs are
recorded on issue #278 after qualification.
