name: Synchronize PR282 with current main

on:
  push:
    branches:
      - industrialize/elastic-contract-fixes

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: industrialize/elastic-contract-fixes
          fetch-depth: 0
      - name: Merge current default branch
        run: |
          git config user.name 'ZEKRITI TAREK'
          git config user.email 'contact@checkupauto.fr'
          git fetch origin main
          git merge --no-edit origin/main
          git rm .github/workflows/autosync-pr282-main.yml
          git commit -m 'chore(ci): remove one-shot main synchronizer'
          git push origin HEAD:industrialize/elastic-contract-fixes
