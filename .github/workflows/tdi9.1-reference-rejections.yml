name: TDI-9.1 reference rejection records

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  reference-rejections:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install pinned Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.97.1
          components: rustfmt,clippy

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run rejection-record Clippy
        run: cargo clippy -p tdi-ai --test tdi9_reference_rejections_compile --locked -- -D warnings

      - name: Verify TDI-9.1 rejection records
        run: bash scripts/check-tdi9.1-reference-rejections.sh
